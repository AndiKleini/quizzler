# quizzler-ui: Angular app served by nginx, 2 replicas.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quizzler-ui
  namespace: quizzler
  labels:
    app: quizzler-ui
spec:
  replicas: 2
  selector:
    matchLabels:
      app: quizzler-ui
  template:
    metadata:
      labels:
        app: quizzler-ui
    spec:
      containers:
        - name: quizzler-ui
          image: quizzler-ui:kind
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: quizzler-ui
  namespace: quizzler
spec:
  selector:
    app: quizzler-ui
  ports:
    - port: 80
      targetPort: 80
