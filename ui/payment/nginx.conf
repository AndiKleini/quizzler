server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: let Angular's router handle deep links
    # (e.g. /payment/{paymentId}).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
